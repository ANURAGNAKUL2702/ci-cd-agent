# Example Configuration for CI/CD Agent

# GitHub Configuration
# Set these as environment variables or in a .env file (never commit .env!)

# Required: GitHub Personal Access Token
# Create at: https://github.com/settings/tokens
# Required permissions: repo, workflow, read:org
GITHUB_TOKEN=your_github_personal_access_token_here

# Required: Repository name in format 'owner/repo'
GITHUB_REPOSITORY=username/repository-name

# Optional Configuration

# Maximum number of failed workflows to analyze at once (default: 5)
MAX_WORKFLOWS=5

# Log level: DEBUG, INFO, WARNING, ERROR (default: INFO)
LOG_LEVEL=INFO

# Log file location (default: cicd_agent.log)
LOG_FILE=cicd_agent.log

# Usage Examples:
#
# 1. Analyze failed workflows:
#    export GITHUB_TOKEN="ghp_xxxxxxxxxxxx"
#    export GITHUB_REPOSITORY="owner/repo"
#    python ci_cd_agent.py analyze
#
# 2. Validate and fix workflow YAML:
#    python ci_cd_agent.py validate .github/workflows/ci.yml --fix
#
# 3. Run tests:
#    pytest tests/ -v
